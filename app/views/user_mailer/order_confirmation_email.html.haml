!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
  %body
    %h1 Order Confirmation
    %p
      Order number: #{@consumer.orders.last.id}
    %p
      Your order has been recieved!
    -if @consumer.user
      %p
        = order_url(@consumer.orders.last.store, @consumer.orders.last.id)
    -else
      %p
        = guest_order_url(@consumer.orders.last.random_order_id)
