= simple_form_for [:store_admin, @category]  do |f|
  = f.error_notification
  = f.input :name, input_html: {class: "span6"}
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = submit_tag 'Cancel', :type => :reset, :class => "btn btn-danger"
  = f.error :base
